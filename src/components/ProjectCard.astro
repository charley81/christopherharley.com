---
import type { Project } from '../data/projects'
export type { Project }
export interface Props {
  project: Project
}

const { project } = Astro.props
---

<a
  href={project.slug}
  class="block relative group cursor-pointer overflow-hidden aspect-[16/9] bg-surface-subtle border border-border-muted"
>
  <img
    alt={project.imageAlt}
    class="w-full h-full object-cover grayscale-filter group-hover:scale-105"
    src={project.imageUrl}
  />
  <div
    class="absolute inset-0 bg-gradient-to-t from-[#181613]/90 via-[#181613]/30 to-transparent flex flex-col justify-end p-gutter"
  >
    <h3 class="font-headline-md text-headline-md text-white mb-2">
      {project.title}
    </h3>
    <span class="text-white font-label-mono text-label-mono mb-4 flex items-center gap-2">
      <span class="group-hover:underline">Read Case Study</span>
      <span class="material-symbols-outlined text-[16px] group-hover:translate-x-1 transition-transform">arrow_forward</span>
    </span>
    <div class="flex gap-2">
      {
        project.tags.map((tag) => (
          <span class="px-3 py-1 border border-white/30 rounded-full text-white text-[10px] uppercase font-label-mono">
            {tag}
          </span>
        ))
      }
    </div>
  </div>
</a>
